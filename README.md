# Resume Detection Project

## 📌 Overview
This project extracts text from resumes (`.pdf` and `.docx`), parses them using NLP (SpaCy), and detects skills using regex matching.  
Results are saved in structured JSON format for easy analysis.

---

## ⚙️ Setup Instructions

1. **Clone the repository** (or create the folder structure in Visual Studio):
   ```bash
   git clone <your-repo-url>
   cd resume_detection
